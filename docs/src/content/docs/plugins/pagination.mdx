---
title: Pagination
description: Dot indicators for Unswipe carousels.
---

import UnswipeRuntime from '../../../components/UnswipeRuntime.astro';
import Playground from '../../../components/Playground.astro';

<UnswipeRuntime />

```ts
import { pagination } from 'unswipe/plugins/pagination';

new Unswipe(root, {}, [pagination()]);
```

One dot per snap group when `slidesToScroll > 1`.

| Option | Default | Description |
| --- | --- | --- |
| `label` | `"Carousel pagination"` | Tablist label |
| `className` | `"unswipe-pagination"` | CSS class prefix |
| `container` | sibling wrapper | Where dots render |

<Playground
  id="plugin-dots"
  label="Pagination demo"
  plugins="navigation,pagination"
  code={`import { pagination } from 'unswipe/plugins/pagination';
import { navigation } from 'unswipe/plugins/navigation';
new Unswipe(root, {}, [navigation(), pagination()]);`}
/>

Guide: [Dot buttons](../../guides/dot-buttons/).
