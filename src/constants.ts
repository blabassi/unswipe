/** Marks a cloned slide used by the loop plugin. */
export const CLONE_ATTR = 'data-unswipe-clone';

/** Logical slide index (on real slides and clones). */
export const INDEX_ATTR = 'data-unswipe-index';
